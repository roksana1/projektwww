<%= render partial: 'articles/menu' %>

<div class="blog">
  <div class="container">
    
    <div class="panel panel-default widoczek">
      
      <div class="articleheader">
        <b><p class="articletitle"><%= @article.title %></p></b>
         <p>
          Tagi:
          <% @article.tags.each do |tag| %>
            <%= link_to tag.name, tag_path(tag) %>
          <% end %>
        </p>
        <% if logged_in? %>
            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span><%= link_to "usuń", article_path(@article), method: :delete, data: {confirm: "Czy na pewno chcesz usunąć wpis?"} %>
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span><%= link_to "edytuj", edit_article_path(@article) %></br>
        <% end %>
      </div>
      
      <div class="zdjecieart">
        <p><%= image_tag @article.image.url %></p>
      </div>
      <div class="artcontent">
        <p><%= @article.body %></p>
      </div>
    </div>
    
    
  <div class="panel panel-default widoczek">
    <div class="commentart">
        <h2>Komentarze (<%= @article.comments.size %>)</h2>
        <row>
            <%= render partial: 'articles/comment', collection: @article.comments.reverse %></br>
        <h4>Podziel się swoją opinią:</h4>
            <%= render partial: 'comments/form' %>
        </row>
    </div>
    <div class="panel-footer">
            <a><span class="glyphicon glyphicon-chevron-left" aria-hidden="true"><%= link_to 'Wróć', articles_path %></span></a>
    </div>
  </div>
  
</div>
</div>